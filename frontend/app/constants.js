//-------------------------
// CONSTANTS
//-------------------------

const CONTENT_TOP = 70 // $navbar-height: (50px) + .content margin-top (20px)

// debug levels
const CRITICAL = 'CRITICAL'
const ERROR = 'ERROR'
const WARNING = 'WARNING'
const INFO = 'INFO'
const DEBUG = 'DEBUG'

module.exports = {
  CONTENT_TOP,
  CRITICAL,
  ERROR,
  WARNING,
  INFO,
  DEBUG,
}
